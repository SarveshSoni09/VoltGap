"""Published artifacts: the files the static frontend actually loads.

Everything here reads the committed outputs of Phases 3, 4 and 5 and writes the artifacts
§12 lists. Nothing here re-derives a model quantity — if a number is not already produced
by an accepted phase, it does not appear in an artifact.
"""
